name: "Go CI & Security"

on:
  push:
    branches: [ "main" ]
    paths:
      - 'packages/pocketgull-go/**'
      - '.github/workflows/go-ci.yml'
      - '.golangci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'packages/pocketgull-go/**'
      - '.github/workflows/go-ci.yml'
      - '.golangci.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Style
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34 # v5.3.0
        with:
          go-version: '1.24.0'
          cache: false

      - name: Go Vet & Format Check
        working-directory: packages/pocketgull-go
        run: |
          go vet ./...
          test -z "$(gofmt -l .)" || true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@2226f7cb6f94fc339db3f00f3949d0f119867828 # v6.5.0
        continue-on-error: true
        with:
          version: latest
          working-directory: packages/pocketgull-go
          args: --timeout=5m

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Harden Runner
        if: runner.os == 'Linux'
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34 # v5.3.0
        with:
          go-version: '1.24.0'
          cache: false

      - name: Verify Module Hygiene
        working-directory: packages/pocketgull-go
        run: |
          go mod tidy

      - name: Run Unit Tests
        working-directory: packages/pocketgull-go
        run: |
          go test -v ./...

  vulncheck:
    name: Vulnerability Scan (govulncheck)
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34 # v5.3.0
        with:
          go-version: 'stable'
          cache: false

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        working-directory: packages/pocketgull-go
        run: govulncheck -mode=binary ./... || true
