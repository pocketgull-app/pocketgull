# Google Cloud Build CI/CD Pipeline for Pocket Gull
# Project: gen-lang-client-0540208645
# Service: pocket-gull (Google Cloud Run)

steps:
  # 1. Build Production Container Image with Docker
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-container'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:$BUILD_ID'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:latest'
      - '.'

  # 2. Push Docker Images to Google Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:$BUILD_ID'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-latest'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:latest'

  # 3. Deploy to Google Cloud Run (us-central1) with Scale-to-Zero Cost Strategy
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'pocket-gull'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:$BUILD_ID'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--update-env-vars=OTEL_SDK_DISABLED=true'

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:$BUILD_ID'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/pocketgull-repo/pocket-gull:latest'

timeout: '3000s'
options:
  machineType: 'E2_HIGHCPU_8'
  logging: 'CLOUD_LOGGING_ONLY'
