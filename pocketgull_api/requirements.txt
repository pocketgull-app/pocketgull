# Pocket Gull — FastAPI Sidecar Requirements
# Install: pip install -r requirements.txt

# Core — required
fastapi==0.141.1
uvicorn==0.52.0
pydantic==2.13.4
numpy==2.5.1

# JAX & Deep Learning Stack
jax>=0.4.30
jaxlib>=0.4.30
flax>=0.9.0
optax>=0.2.2
orbax-checkpoint>=0.5.15

# Export & Math Utilities
onnx>=1.16.0

# DataFrame ingestion (P1)
pandas==3.0.5

# FHIR Bundle validation gateway (P1)
fhir.resources==8.3.0

# HDF5 biosignal archive reader (P2)
h5py==3.8.0
scipy==1.18.0

# ML risk scoring (P3 — only needed when a trained model is present)
joblib==1.5.3
scikit-learn==1.9.0

# File uploads (multipart/form-data support)
python-multipart==0.0.32

# Development
httpx==0.28.1

